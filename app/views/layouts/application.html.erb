<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="<%= content_for?(:description) ? content_for(:description) : @settings.about_lead %>">
    <meta name="author" content="<%= @settings.author_name %>">
    <title><%= content_for?(:title) ? content_for(:title) : @settings.blog_title %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
    <% unless @settings.google_analytics == "" %>
      <%= render 'layouts/google_analytics' %>
    <% end %>
  </head>

  <body>
    <%= render 'shared/nav' %>
    <%= render 'shared/header' %>
    <div class="container">
      <%= render 'shared/alerts' %>
      <div class="row">
        <div class="col-sm-9 blog-main">
          <%= yield %>
        </div>
        <%= render 'shared/sidebar' %>
      </div>
    </div>

    <%= render 'shared/footer' %>
    </div>

  </body>
</html>
